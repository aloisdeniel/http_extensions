name: http_extensions_retry
description: An http extension that retries failed requests.
version: 1.0.0
homepage: https://github.com/aloisdeniel/http_extensions

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  meta: ^1.3.0
  http: ^0.13.3
  http_extensions: ^1.0.0
    #path: ../http_extensions
  logging: ^1.0.1
