name: http_extensions
description: Base classes for building extensions for http package.
version: 1.0.0
homepage: https://github.com/aloisdeniel/http_extensions

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  meta: ^1.3.0
  http: ^0.13.0